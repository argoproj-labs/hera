[tox]
envlist = flake8-py3, py37, py38, py39, py310

[testenv]
deps = pipenv
commands=
    pipenv install --dev
    pipenv run make test

[testenv:flake8-py3]
basepython = python3.7
commands=
    pipenv install --dev
    pipenv run make lint typecheck
